# ── Base de datos ──────────────────────────────────────────────────────────────
# Si usas el PostgreSQL del docker-compose, usa esta URL interna:
DATABASE_URL="postgresql://postgres:1Qaz2wsx%23@bd-bdclientes-4nlmdx:5432/inventario-churco"

# Si tu PostgreSQL ya corre en el servidor (sin Docker):
# DATABASE_URL="postgresql://user:password@localhost:5432/groom_gold"

# ── NextAuth ───────────────────────────────────────────────────────────────────
# Genera el secret con: openssl rand -base64 32
NEXTAUTH_SECRET="tu-secret-aqui"
NEXTAUTH_URL="http://TU_IP_O_DOMINIO"

# ── PostgreSQL (solo si usas el servicio db del docker-compose) ───────────────
POSTGRES_USER=groom
POSTGRES_PASSWORD=groompassword
POSTGRES_DB=groom_gold

# ── Google Sheets (opcional) ──────────────────────────────────────────────────
GOOGLE_SERVICE_ACCOUNT_EMAIL="tu-cuenta@proyecto.iam.gserviceaccount.com"
GOOGLE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"
GOOGLE_SHEET_ID="id-de-tu-google-sheet"
